# Sao chép file này thành .env rồi điền giá trị thật.

# URL của backend API. Backend có global prefix `api`, nên trỏ thẳng vào /api.
VITE_API_URL=http://localhost:3000/api

# URL của WebSocket (socket.io). Phải là host gốc (không kèm /api).
VITE_SOCKET_URL=http://localhost:3000
